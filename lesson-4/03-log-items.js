const logItems = function (items) {
	for (const item of items) {
		console.log(item);
	}
};

logItems(['Sofa', 'Afgust', 'Mark', 'Nastiy']);
logItems([1, 2, 3, 4, 5,]);
logItems(['Pasta', 'Pateto', 'Beef']);